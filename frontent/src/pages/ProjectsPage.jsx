import { useState } from "react";
import { CirclePlus, Plus } from "lucide-react";
import { NewProjectModal } from "../components/project/NewProjectModal";
import { ProjectCard } from "../components/project/ProjectCard";
export function ProjectsPage({ projects, onOpen, onCreate }) { const [isCreating, setCreating] = useState(false); const createProject = async (details) => { const project = await onCreate(details); setCreating(false); onOpen(project.id); }; return <main className="page dashboard"><div className="page-heading"><div><p className="eyebrow">Workspace</p><h1>Projects</h1><p>Organise your work into focused boards.</p></div><button className="primary-button" onClick={() => setCreating(true)}><Plus size={17} /> New project</button></div>{projects.length ? <div className="project-grid">{projects.map((project) => <ProjectCard key={project.id} project={project} onOpen={onOpen} />)}</div> : <div className="empty-state"><CirclePlus size={30} /><h2>Your workspace is empty</h2><p>Create your first project and start moving work forward.</p><button className="primary-button" onClick={() => setCreating(true)}>Create project</button></div>}{isCreating && <NewProjectModal onCreate={createProject} onClose={() => setCreating(false)} />}</main>; }
